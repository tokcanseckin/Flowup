# FlowUp Alignment Worker — Python dependencies
# Install via: pip install -r requirements.txt  (done automatically by install.sh)

requests>=2.31.0
python-dotenv>=1.0.0
yt-dlp>=2024.1.1
stable-ts>=2.19.0

# ── Pipeline dependencies (worker runs generate_song_data.py locally) ─────────
transliterate>=1.10.2
pymorphy3>=2.0.2
ruaccent>=0.0.10
onnxruntime>=1.17.0
argostranslate>=1.9.6
nltk>=3.8.0

# torch, demucs, and ffmpeg are installed separately by install.sh
# because they require platform-specific commands on macOS
